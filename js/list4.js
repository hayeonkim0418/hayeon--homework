import { newsList } from "../data/list4-news-data.js";
import { infoList } from "../data/list4-info-data.js";

newsList();
infoList();

// const tabsButton = document.querySelectorAll("button");

// tabsButton.forEach((tab) => {
//   let toggle = true;
//   tab.addEventListener("click", () => {
//     if (toggle) {
//       tab.setAttribute("aria-selected", "false");
//     } else {
//       tab.setAttribute("aria-selected", "true");
//     }
//   });
//   toggle = !toggle;
// });
